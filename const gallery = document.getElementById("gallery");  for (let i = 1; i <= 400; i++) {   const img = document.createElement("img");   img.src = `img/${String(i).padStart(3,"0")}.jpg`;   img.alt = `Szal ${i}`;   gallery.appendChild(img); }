const gallery = document.getElementById("gallery");

for (let i = 1; i <= 400; i++) {
  const img = document.createElement("img");
  img.src = `img/${String(i).padStart(3,"0")}.jpg`;
  img.alt = `Szal ${i}`;
  gallery.appendChild(img);
}
